/*global define*/
/*jslint white:true,browser:true*/

define([
    
], function() {
    'use strict';

    function factory(config) {
    }

    return {
        make: function (config) {
            return factory(config);
        }
    };
});