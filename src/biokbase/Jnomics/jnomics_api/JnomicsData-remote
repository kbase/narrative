#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.1)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

from jnomics_thrift_api import JnomicsData
from jnomics_thrift_api.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  JnomicsThriftHandle create(string path, Authentication auth)'
  print '  JnomicsThriftHandle open(string path, Authentication auth)'
  print '  void write(JnomicsThriftHandle handle, string data, Authentication auth)'
  print '  string read(JnomicsThriftHandle handle, Authentication auth)'
  print '  void close(JnomicsThriftHandle handle, Authentication auth)'
  print '   listStatus(string path, Authentication auth)'
  print '  bool checkFileStatus(string path, Authentication auth)'
  print '   listShockStatus(string path, Authentication auth)'
  print '  bool remove(string path, bool recursive, Authentication auth)'
  print '  bool mkdir(string path, Authentication auth)'
  print '  bool mv(string path, string dest, Authentication auth)'
  print '   listGenomes(Authentication auth)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = JnomicsData.Client(protocol)
transport.open()

if cmd == 'create':
  if len(args) != 2:
    print 'create requires 2 args'
    sys.exit(1)
  pp.pprint(client.create(args[0],eval(args[1]),))

elif cmd == 'open':
  if len(args) != 2:
    print 'open requires 2 args'
    sys.exit(1)
  pp.pprint(client.open(args[0],eval(args[1]),))

elif cmd == 'write':
  if len(args) != 3:
    print 'write requires 3 args'
    sys.exit(1)
  pp.pprint(client.write(eval(args[0]),args[1],eval(args[2]),))

elif cmd == 'read':
  if len(args) != 2:
    print 'read requires 2 args'
    sys.exit(1)
  pp.pprint(client.read(eval(args[0]),eval(args[1]),))

elif cmd == 'close':
  if len(args) != 2:
    print 'close requires 2 args'
    sys.exit(1)
  pp.pprint(client.close(eval(args[0]),eval(args[1]),))

elif cmd == 'listStatus':
  if len(args) != 2:
    print 'listStatus requires 2 args'
    sys.exit(1)
  pp.pprint(client.listStatus(args[0],eval(args[1]),))

elif cmd == 'checkFileStatus':
  if len(args) != 2:
    print 'checkFileStatus requires 2 args'
    sys.exit(1)
  pp.pprint(client.checkFileStatus(args[0],eval(args[1]),))

elif cmd == 'listShockStatus':
  if len(args) != 2:
    print 'listShockStatus requires 2 args'
    sys.exit(1)
  pp.pprint(client.listShockStatus(args[0],eval(args[1]),))

elif cmd == 'remove':
  if len(args) != 3:
    print 'remove requires 3 args'
    sys.exit(1)
  pp.pprint(client.remove(args[0],eval(args[1]),eval(args[2]),))

elif cmd == 'mkdir':
  if len(args) != 2:
    print 'mkdir requires 2 args'
    sys.exit(1)
  pp.pprint(client.mkdir(args[0],eval(args[1]),))

elif cmd == 'mv':
  if len(args) != 3:
    print 'mv requires 3 args'
    sys.exit(1)
  pp.pprint(client.mv(args[0],args[1],eval(args[2]),))

elif cmd == 'listGenomes':
  if len(args) != 1:
    print 'listGenomes requires 1 args'
    sys.exit(1)
  pp.pprint(client.listGenomes(eval(args[0]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
