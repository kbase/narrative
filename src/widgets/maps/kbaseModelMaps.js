(function( $, undefined ) {

$.KBWidget({
    name: "kbaseModelMaps",     
    version: "1.0.0",
    options: {
    },
    
    init: function(options) {
        var self = this;
        this._super(options);
        var container = this.$elem;






        container.append(table);

        return this;

    }  //end init

})
}( jQuery ) );
