define(['kbaseMSA'], (Widget) => {
    'use strict';
    describe('The kbaseMSA widget', () => {
        it('should be defined', () => {
            expect(Widget).toBeDefined();
        });
    });
});
